#include <stdio.h>
void main()
{
    int valor, soma = 0, resposta = 1;
    while(reposta == 1) //ENQUANTO NAO PARECER O "1" ELE CONTINUA A SOMAR
    {
        scanf("%d", &valor);
        soma += valor;
        scanf("%d", &respota);//COLOCAR 1 PARA CONTINUAR A SOMA OU 0 PARA PARAR DE SOMA E DAR A RESPOTA QUE SOBROU
    }


}
